import { promptFor, promptType } from './tools';
export declare const PrompTools: {
    for: typeof promptFor;
    type: typeof promptType;
};
export declare const PT: {
    for: typeof promptFor;
    type: typeof promptType;
};
