#! /usr/bin/env yarn ts-node
import * as fs from 'fs/promises';
import prompts from 'prompts';

require.main === module && main();
async function main() {
  console.log("You're all done, you can now use daily");
}
